#pragma once
// Generated by tools/gen-items.py -- do not edit.
//
// Host-side plain struct + array mirroring the packed mhItems blob. Field
// order matches the blob byte order; the host reads members directly, so
// host struct padding is irrelevant. The device reads the blob with the
// offsets in items_meta.hpp instead.

#include <array>
#include <stdint.h>

namespace item_data {

constexpr uint8_t VERSION = 1;
constexpr uint16_t BLOB_SIZE = 48;

struct Item {
    uint8_t kind;   // item::KIND_CONSUMABLE / KIND_MATERIAL
    uint8_t heal;
    uint8_t stam;
    uint16_t sell;
};

inline constexpr std::array<Item, 8> ITEMS = {{
    {0, 20, 0, 10},   // herb
    {0, 10, 0, 15},   // blue_mushroom
    {1, 0, 0, 30},   // ore
    {1, 0, 0, 5},   // bug
    {1, 0, 0, 25},   // scale
    {1, 0, 0, 40},   // shell
    {1, 0, 0, 60},   // fang
    {1, 0, 0, 80},   // tail
}};

}   // namespace item_data
