#pragma once
// Generated by tools/gen-quests.py -- do not edit.
//
// Quest data ABI (docs/quests-shops.md): header then one fixed 9 B
// QuestDef record per quest, ordered by id. src/quest.hpp reads this
// blob through core/fxmem.hpp during the screen scan/render window;
// src/quest_state.hpp holds the host-testable logic + struct.

#include <stdint.h>
#include "../core/progmem.hpp"   // MH_PROGMEM: QUEST_ROOM_HINT lives in flash on AVR

namespace quests {

constexpr uint16_t MAGIC = 0x5153;
constexpr uint8_t VERSION = 2;
constexpr uint8_t FLAGS = 0x00;
constexpr uint16_t SIZE = 53;
constexpr uint8_t HEADER_SIZE = 8;
constexpr uint8_t RECORD_SIZE = 9;
constexpr uint8_t QUEST_COUNT = 5;

// Record field offsets (QuestDef: id, goalKind, target, need, rewardZenny,
// rewardItem, rewardCount, unlockFlag).
constexpr uint8_t DEF_ID_OFF = 0;
constexpr uint8_t DEF_GOAL_OFF = 1;
constexpr uint8_t DEF_TARGET_OFF = 2;
constexpr uint8_t DEF_NEED_OFF = 3;
constexpr uint8_t DEF_REWARD_ZENNY_OFF = 4;   // u16
constexpr uint8_t DEF_REWARD_ITEM_OFF = 6;    // itemIdx + 1, 0 = none
constexpr uint8_t DEF_REWARD_COUNT_OFF = 7;
constexpr uint8_t DEF_UNLOCK_OFF = 8;

// Goal kinds; values mirror the packed goalKind byte.
constexpr uint8_t GOAL_KILL = 0;
constexpr uint8_t GOAL_GATHER = 1;

// Kill target kinds; values mirror MonsterKind in src/core/game.hpp.
// A gather quest's target is instead an item index (data/items.json).
constexpr uint8_t TARGET_LUNGE = 0;
constexpr uint8_t TARGET_SWEEP = 1;
constexpr uint8_t TARGET_HEAVY = 2;
constexpr uint8_t TARGET_RAVAGER = 3;
constexpr uint8_t TARGET_POLE = 4;

// MAP screen quest marker (monhun-ardu-imx): the room each quest points
// at, in quest index order (sorted by id). Values index the room order
// data/map.json is packed in (sorted by id == zone::ROOM_*, see
// generated/zone_meta.hpp); QUEST_ROOM_HINT_NONE = no marker. The hint
// is authored per quest (data/quests/*.json `roomHint`) and never packed
// into the mhQuests blob. Read it with mhPgmReadU8 (flash on AVR).
constexpr uint8_t QUEST_ROOM_HINT_NONE = 0xFF;
constexpr uint8_t MAP_ROOM_COUNT = 4;
MH_PROGMEM constexpr uint8_t QUEST_ROOM_HINT[QUEST_COUNT] = {0x00, 0x00, 0x02, 0x03, 0x00};

// Quest indices, sorted by id, with the cart record offsets the runtime uses.
constexpr uint8_t QUEST_SLAY_LUNGE = 0;
constexpr uint16_t QUEST_SLAY_LUNGE_OFF = 8;
constexpr uint8_t QUEST_SLAY_SWEEP = 1;
constexpr uint16_t QUEST_SLAY_SWEEP_OFF = 17;
constexpr uint8_t QUEST_GATHER_ORE = 2;
constexpr uint16_t QUEST_GATHER_ORE_OFF = 26;
constexpr uint8_t QUEST_CRUSH_HEAVY = 3;
constexpr uint16_t QUEST_CRUSH_HEAVY_OFF = 35;
constexpr uint8_t QUEST_TRAIN_POLE = 4;
constexpr uint16_t QUEST_TRAIN_POLE_OFF = 44;

}   // namespace quests
