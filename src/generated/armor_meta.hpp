#pragma once
// Generated by tools/gen-armor.py -- do not edit.
//
// Armor table ABI (bead monhun-ardu-arm.1): 8 B header + one fixed 18 B
// piece record + one fixed 3 B skill record, little-endian, no padding.
// Offsets are absolute byte offsets into the mhArmor raw_t section
// (fxdata/fxdata.txt); on AVR the loader reads mhArmor + off through
// core/fxmem.hpp. Piece/skill index == record index; armor::mat::<ID>
// indexes the mhItems table (item index + 1 in a recipe slot).

#include <stdint.h>

namespace armor {

constexpr uint16_t MAGIC = 0x5241;
constexpr uint8_t VERSION = 1;
constexpr uint8_t FLAGS = 0x00;
constexpr uint16_t SIZE = 113;
constexpr uint8_t HEADER_SIZE = 8;
constexpr uint8_t PIECE_SIZE = 18;
constexpr uint8_t SKILL_SIZE = 3;
constexpr uint8_t PIECE_COUNT = 5;
constexpr uint8_t SKILL_COUNT = 5;
constexpr uint8_t PIECE_MAX = 32;
constexpr uint8_t MAT_SLOTS = 2;    // packed {item,count} pairs per piece
constexpr uint8_t SKILL_SLOTS = 2;  // packed {skill,points} pairs per piece
constexpr uint16_t PIECES_OFF = 8;
constexpr uint16_t SKILLS_OFF = 98;
constexpr uint8_t SHEET_NONE = 0;

// Piece slots; values mirror the packed slot byte.
constexpr uint8_t SLOT_HEAD = 0;
constexpr uint8_t SLOT_BODY = 1;
constexpr uint8_t SLOT_CHARM = 2;

// Skill effect kinds; values mirror the packed kind byte.
constexpr uint8_t KIND_ATTACK_UP = 0;
constexpr uint8_t KIND_DEFENSE_UP = 1;
constexpr uint8_t KIND_HEALTH_UP = 2;
constexpr uint8_t KIND_STAMINA_UP = 3;
constexpr uint8_t KIND_EVADE_WINDOW = 4;

// Piece field offsets (slot, defense, resist, zenny, mat, sheet, skills).
constexpr uint8_t PIECE_SLOT_OFF = 0;
constexpr uint8_t PIECE_DEFENSE_OFF = 1;
constexpr uint8_t PIECE_RESIST_OFF = 2;    // 4 x i8: fire, water, ice, thunder
constexpr uint8_t PIECE_ZENNY_OFF = 6;     // u16
constexpr uint8_t PIECE_MAT_OFF = 8;       // MAT_SLOTS x (itemIdx+1 u8, count u8)
constexpr uint8_t PIECE_MAT_STRIDE = 2;
constexpr uint8_t PIECE_SHEET_OFF = 12;    // sheet index + 1, 0 = none
constexpr uint8_t PIECE_SKILL_COUNT_OFF = 13;
constexpr uint8_t PIECE_SKILLS_OFF = 14;   // SKILL_SLOTS x (skillIdx+1 u8, points u8)
constexpr uint8_t PIECE_SKILL_STRIDE = 2;

// Skill field offsets (kind, maxPoints, perPoint).
constexpr uint8_t SKILL_KIND_OFF = 0;
constexpr uint8_t SKILL_MAX_OFF = 1;
constexpr uint8_t SKILL_PER_POINT_OFF = 2;

// Shared activation thresholds (data/skills.json): total points across the
// equipped pieces below THRESHOLD_S are inert; >= THRESHOLD_S activates the
// skill; THRESHOLD_M is the max useful total (see docs/equipment-framework.md).
constexpr uint8_t THRESHOLD_S = 10;
constexpr uint8_t THRESHOLD_M = 15;

// Armor piece indices + blob record offsets, in data/armor.json source order.
constexpr uint8_t ARMOR_HUNTER_HELM = 0;
constexpr uint16_t ARMOR_HUNTER_HELM_OFF = 8;
constexpr uint8_t ARMOR_BONE_CAP = 1;
constexpr uint16_t ARMOR_BONE_CAP_OFF = 26;
constexpr uint8_t ARMOR_HUNTER_MAIL = 2;
constexpr uint16_t ARMOR_HUNTER_MAIL_OFF = 44;
constexpr uint8_t ARMOR_BONE_MAIL = 3;
constexpr uint16_t ARMOR_BONE_MAIL_OFF = 62;
constexpr uint8_t ARMOR_EVADE_CHARM = 4;
constexpr uint16_t ARMOR_EVADE_CHARM_OFF = 80;

// Skill indices + blob record offsets, in data/skills.json source order.
constexpr uint8_t SKILL_ATTACK_UP = 0;
constexpr uint16_t SKILL_ATTACK_UP_OFF = 98;
constexpr uint8_t SKILL_DEFENSE_UP = 1;
constexpr uint16_t SKILL_DEFENSE_UP_OFF = 101;
constexpr uint8_t SKILL_HEALTH_UP = 2;
constexpr uint16_t SKILL_HEALTH_UP_OFF = 104;
constexpr uint8_t SKILL_STAMINA_UP = 3;
constexpr uint16_t SKILL_STAMINA_UP_OFF = 107;
constexpr uint8_t SKILL_EVADE_WINDOW = 4;
constexpr uint16_t SKILL_EVADE_WINDOW_OFF = 110;

// Sheet symbols referenced by pieces (placeholder ids until the sprite
// epic lands); the packed sheet field is this index. 0 = no sheet.
namespace sheet {
constexpr uint8_t NONE = 0;
constexpr uint8_t MH_BODY_BONE_MAIL = 1;
constexpr uint8_t MH_BODY_HUNTER_MAIL = 2;
constexpr uint8_t MH_CHARM_EVADE_CHARM = 3;
constexpr uint8_t MH_HEAD_BONE_CAP = 4;
constexpr uint8_t MH_HEAD_HUNTER_HELM = 5;
}   // namespace sheet

// Recipe material item ids (index into Game::items[] / the mhItems table).
namespace mat {
constexpr uint8_t HERB = 0;
constexpr uint8_t BLUE_MUSHROOM = 1;
constexpr uint8_t ORE = 2;
constexpr uint8_t BUG = 3;
constexpr uint8_t SCALE = 4;
constexpr uint8_t SHELL = 5;
constexpr uint8_t FANG = 6;
constexpr uint8_t TAIL = 7;
}   // namespace mat

}   // namespace armor
