#pragma once
// Generated by tools/gen-smith.py -- do not edit.
//
// Smith data ABI (docs/quests-shops.md): header, then one fixed 11 B
// UpgradeDef record per weapon tier ordered by (weaponIdx, tier), then one
// fixed 8 B armor recipe record per data/armor.json piece in source order.
// src/smith.hpp reads the weapon records through core/fxmem.hpp during the
// screen scan/render window; src/upgrade_state.hpp holds the host-testable
// struct + integer-percent multiplier math + the recipe debits (prg.7
// materials). The armor recipe array is data for the arm.2 craft UI, which
// will debit it with the same {itemIdx+1, count} + zenny rule.

#include <stdint.h>

namespace smith {

constexpr uint16_t MAGIC = 0x534D;
constexpr uint8_t VERSION = 3;
constexpr uint8_t FLAGS = 0x00;
constexpr uint16_t SIZE = 114;
constexpr uint8_t HEADER_SIZE = 8;
constexpr uint8_t RECORD_SIZE = 11;
constexpr uint8_t UPGRADE_COUNT = 6;
constexpr uint8_t TIER_COUNT = 2;
constexpr uint8_t WEAPON_COUNT = 3;
constexpr uint8_t MAT_SLOTS = 2;   // packed {item,count} pairs per record
constexpr uint8_t ARMOR_RECIPE_SIZE = 8;
constexpr uint8_t ARMOR_RECIPE_COUNT = 5;
constexpr uint16_t ARMOR_RECIPES_OFF = 74;

// Record field offsets (UpgradeDef: weaponIdx, tier, cost, dmgMul, spdMul,
// unlockFlag, mat[MAT_SLOTS]).
constexpr uint8_t UPG_WEAPON_OFF = 0;
constexpr uint8_t UPG_TIER_OFF = 1;
constexpr uint8_t UPG_COST_OFF = 2;   // u16
constexpr uint8_t UPG_DMG_OFF = 4;
constexpr uint8_t UPG_SPD_OFF = 5;
constexpr uint8_t UPG_UNLOCK_OFF = 6;
constexpr uint8_t UPG_MAT_OFF = 7;    // MAT_SLOTS x (itemIdx+1 u8, count u8)
constexpr uint8_t UPG_MAT_STRIDE = 2;

// Armor recipe field offsets (armorIdx u8, cost u16, unlockFlag u8, mat[2]).
constexpr uint8_t AREC_ARMOR_OFF = 0;
constexpr uint8_t AREC_COST_OFF = 1;    // u16
constexpr uint8_t AREC_UNLOCK_OFF = 3;
constexpr uint8_t AREC_MAT_OFF = 4;     // MAT_SLOTS x (itemIdx+1 u8, count u8)
constexpr uint8_t AREC_MAT_STRIDE = 2;

// Weapon indices; values mirror WeaponId in src/core/game.hpp.
constexpr uint8_t WEAPON_SWORD = 0;
constexpr uint8_t WEAPON_FLAIL = 1;
constexpr uint8_t WEAPON_GUN = 2;

// Upgrade indices, sorted by (weapon, tier), with the cart record offsets.
constexpr uint8_t UPG_SWORD_T1 = 0;
constexpr uint16_t UPG_SWORD_T1_OFF = 8;
constexpr uint8_t UPG_SWORD_T2 = 1;
constexpr uint16_t UPG_SWORD_T2_OFF = 19;
constexpr uint8_t UPG_FLAIL_T1 = 2;
constexpr uint16_t UPG_FLAIL_T1_OFF = 30;
constexpr uint8_t UPG_FLAIL_T2 = 3;
constexpr uint16_t UPG_FLAIL_T2_OFF = 41;
constexpr uint8_t UPG_GUN_T1 = 4;
constexpr uint16_t UPG_GUN_T1_OFF = 52;
constexpr uint8_t UPG_GUN_T2 = 5;
constexpr uint16_t UPG_GUN_T2_OFF = 63;

// Armor recipe indices, in data/armor.json source order (== the
// armor::ARMOR_<ID> piece index), with the cart record offsets.
constexpr uint8_t AREC_HUNTER_HELM = 0;
constexpr uint16_t AREC_HUNTER_HELM_OFF = 74;
constexpr uint8_t AREC_BONE_CAP = 1;
constexpr uint16_t AREC_BONE_CAP_OFF = 82;
constexpr uint8_t AREC_HUNTER_MAIL = 2;
constexpr uint16_t AREC_HUNTER_MAIL_OFF = 90;
constexpr uint8_t AREC_BONE_MAIL = 3;
constexpr uint16_t AREC_BONE_MAIL_OFF = 98;
constexpr uint8_t AREC_EVADE_CHARM = 4;
constexpr uint16_t AREC_EVADE_CHARM_OFF = 106;

// Recipe material item ids (index into Game::items[] / the mhItems table).
namespace mat {
constexpr uint8_t HERB = 0;
constexpr uint8_t BLUE_MUSHROOM = 1;
constexpr uint8_t ORE = 2;
constexpr uint8_t BUG = 3;
constexpr uint8_t SCALE = 4;
constexpr uint8_t SHELL = 5;
constexpr uint8_t FANG = 6;
constexpr uint8_t TAIL = 7;
}   // namespace mat

}   // namespace smith
